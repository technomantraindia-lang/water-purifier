// NOTE: Frontend is fully static. All data comes from local static files:
// - src/data/products-data.js  (categories & products)
// - src/data/blog-data.js      (blog posts)
//
// The Laravel backend (backend/) is a separate API-only project managed
// for future use. The frontend intentionally does NOT connect to it.
//
// This file intentionally left unused. To re-enable API integration later,
// re-export API functions here and remove the static-data imports from pages.
export {};